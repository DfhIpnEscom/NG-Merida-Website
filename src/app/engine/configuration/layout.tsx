'use client'

import * as React from "react";

export default function EngineConfigurationLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div>
      {children}
    </div>
  );
};